## **工作周报 (2.02 - 2.06)**

### **一、 本周进展**

- PA1 基础设施构建：
  - 完成SDB核心开发，支持 `si`、`info r`、`x` ,`w`等指令。
  - 实现基于递归求值的表达式引擎，支持算术/逻辑运算及解引用，处理了多义符歧义。
  - 建立监视点池管理机制，提升调试自动化水平。
- 系统原理复盘：
  - 以状态机视角梳理 `cpu_exec` 指令执行循环，明确了从取指到执行的状态转移逻辑。
  - 完成 `nemu/` 代码量化统计（`git diff`），通过 `-Wall -Werror` 消除潜在风险，提升系统稳定性。

### **二、 感悟**

- 工具效能：深刻体会到调试基础设施对复杂系统开发的 ROI 提升。
- 建模思维：将程序运行抽象为状态机，为后续 PA2 处理复杂指令集奠定了理论基础。

### **三、 后续计划**

1. 启动 PA2 实验：查阅 i386 手册，解析 Opcode Map。
2. 指令实现：开始编写 x86 核心指令的译码与执行函数。